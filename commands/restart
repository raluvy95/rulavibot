const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
    const token = `${process.env.BOT_TOKEN}`
    if (message.author.id == "390540063609454593")
        message.react('✅')
        .then(message => client.destroy())
        .then(() => client.login(token))
}


module.exports.help = {
    name: "restart"
}
